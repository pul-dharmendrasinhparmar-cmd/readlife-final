# ReadLife book recommendations (OpenAI-compatible)
# Copy to .env.local for local dev, then paste your real key:
#   cp .env.example .env.local
#
# For Netlify deploys: Site settings → Environment variables → add the same names.
# Do NOT put real keys in git.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
